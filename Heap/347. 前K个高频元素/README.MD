# 347. 前K个高频元素

问题描述（中文）：给定一个整数数组 nums 和整数 k，返回数组中出现频率前 k 高的元素。答案可以按任意顺序返回。

解题思路（中文）：
- 使用哈希统计频次，然后使用堆（最小堆维护 k 个高频元素）或直接使用 `Counter.most_common(k)`。
- 堆解法：遍历 (num, freq)，将 (-freq, num) 或使用 (freq, num) 与最小堆配合维护 k 个元素。

复杂度：时间 O(n log k)，空间 O(n)。
