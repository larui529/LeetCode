# 215. 数组中的第K个最大元素

问题描述（中文）：给定整数数组 nums 和整数 k，返回数组中第 k 个最大的元素（不是第 k 个不同的元素）。

解题思路（中文）：
- 使用堆（heapq）：维护一个大小为 k 的最小堆，遍历数组将元素推入堆，堆超过 k 时弹出最小元素，最终堆顶即为第 k 大。
- 也可直接使用 `heapq.nlargest(k, nums)[-1]` 或基于快速选择（Quickselect）的 O(n) 平均方法。

复杂度：时间 O(n log k)，空间 O(k)。
