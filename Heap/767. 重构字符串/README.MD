# 767. 重构字符串

问题描述（中文）：给定一个字符串 s，将字符串重新排列使得相同字符之间的距离至少为 1（即没有两个相邻字符相同）。若无法重新排列返回空字符串。

解题思路（中文）：
- 使用贪心 + 堆：统计字符频次，使用最大堆（按频次）每次取两个出现频率最高的字符放入结果，然后把它们的频次减少后重新入堆。
- 也可以在每次取出一个字符后暂存，等下次再入堆（避免立即重复使用）。

复杂度：时间 O(n log A)（A 为字符种类），空间 O(A)。
