# 3. 无重复字符的最长子串

问题描述（中文）：给定一个字符串 s，请你找出其中不含有重复字符的最长子串的长度。

解题思路（中文）：
- 使用滑动窗口（双指针）维护当前窗口内不重复的字符。使用字典或数组记录字符上一次出现的位置。
- 当发现重复字符时，将左指针移动到重复字符上一次出现位置的下一位，保证窗口无重复。
- 时间复杂度：O(n)，空间复杂度：O(min(n, charset))。
