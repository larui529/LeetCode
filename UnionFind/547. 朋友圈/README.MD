# 547. 朋友圈

问题描述（中文）：给定一个 n x n 的对称矩阵 isConnected，其中 isConnected[i][j] = 1 表示第 i 个城市和第 j 个城市直接相连，0 表示不直接相连。省份（朋友圈）是连接分量的数量（直接或间接相连）。

解题思路（中文）：
- 使用并查集（Union-Find）把直接相连的城市合并为同一集合，最后统计不同根的数量即为省份数。
- 并查集要实现路径压缩以加速查找。

复杂度：O(n^2 α(n))，空间 O(n)。
