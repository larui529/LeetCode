# 721. 账户合并

问题描述（中文）：给定多条账户信息，每条包含用户名和若干邮箱地址，如果两个账户有共同邮箱，则认为属于同一用户，合并这些账户并输出合并后的账户（用户名 + 排序去重的邮箱列表）。

解题思路（中文）：
- 对每个邮箱维护其首次出现的账户索引（或代表），看到相同邮箱时将对应账户索引在并查集中合并；遍历完后按根将邮箱集合聚合，再构造输出（用户名 + 排序邮箱）。

复杂度：O(A α(n))，A 为邮箱总数。空间 O(A)。
