# 1202. 交换字符串的最小索引

问题描述（中文）：给定字符串 s 和成对的可交换索引 pairs，任意次数交换 pairs 中任意一对位置的字符，返回能得到的按字典序最小的字符串。

解题思路（中文）：
- 使用并查集合并可互相到达的索引，组成连通分量。在每个分量内收集索引并对对应字符排序，然后把最小字符分配到最小索引位置，从而得到字典序最小字符串。

复杂度：O(n log n)（排序分量内字符），空间 O(n)。
